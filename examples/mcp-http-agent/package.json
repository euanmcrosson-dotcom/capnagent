{
  "name": "@capnagent-examples/mcp-http-agent",
  "version": "0.0.1",
  "private": true,
  "description": "Second real-world consumer of @capnagent/mcp: an origin-scoped HTTP agent. Wraps a fetch-style MCP client through capnagent so GETs to allowlisted origins are allowed, while non-allowlisted origins and any write method (POST/PUT/DELETE) are denied before fetch runs.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "demo": "tsx src/bin/demo.ts",
    "regen-purple-evidence-05": "tsx src/bin/regen-purple-evidence-05.ts",
    "lint": "biome check src",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@capnagent/core": "*",
    "@capnagent/mcp": "*"
  },
  "devDependencies": {
    "@biomejs/biome": "1.9.4",
    "@types/node": "20.16.11",
    "tsx": "^4.21.0",
    "typescript": "5.6.3",
    "vite-plugin-top-level-await": "^1.2.2",
    "vite-plugin-wasm": "^3.6.0",
    "vitest": "^2.1.9"
  }
}
