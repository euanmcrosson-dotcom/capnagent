{
  "schema": "mcp-recon/v0.1/caveats",
  "plans": [
    {
      "tool": "list_directory",
      "recommend": "scope",
      "caveats": [
        "tool == \"list_directory\"",
        "arg.path starts_with \"/sandbox/\""
      ],
      "provenance": ["r1", "r3"],
      "note": "Filesystem read tool. mcp-recon scopes it to `tool == \"list_directory\"`; the operator tightened it to the sandbox prefix to prevent disclosure of OS files."
    },
    {
      "tool": "send_email",
      "recommend": "scope",
      "caveats": [
        "tool == \"send_email\"",
        "arg.to starts_with \"alice@\""
      ],
      "provenance": ["r1", "r3"],
      "note": "Outbound side-effect with high blast radius. mcp-recon scopes the tool; the operator tightened it to a known-correspondent prefix. Body-level checks are handled by mcp-guardrails on top of this."
    }
  ]
}
