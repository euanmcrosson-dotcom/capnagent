# wasm-pack output. Generated by `npm run build:wasm` (see
# scripts/build-wasm.{sh,cmd} for the underlying invocation). The
# artefact is reproducible from source on every CI run, so it does
# not belong in the repo — and committing it would cause merge churn
# every time the wasm-bindgen toolchain version bumps.
pkg/
